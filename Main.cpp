#include <iostream>
#include "ListaEnlazada.h"

using namespace std;

int main() {

	ListaEnlazada lista;
	lista.agregar(1);
	lista.agregar(2);
	lista.agregar(4);
	lista.agregar(5);
	lista.imprimir();


	//cout << *apt;
	return 0;
}
